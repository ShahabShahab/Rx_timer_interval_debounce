<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.sample.screensaverapp">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".ScreenSaverActivity"
        android:launchMode="singleInstance">
        </activity>

        <uses-library
                android:name="com.google.android.things"
                android:required="false"/>

        <activity android:name=".SecondScreenActivity"
        android:launchMode="singleInstance">
        </activity>
        <activity android:name=".BaseScreenSaverActivityUsingTimeTask">
        </activity>
        <activity android:name=".BaseScreenSaverActivityUsingRxOperator">
        </activity>
        <activity android:name=".FirstScreenActivity"
        android:launchMode="singleInstance">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>